### definitions ###

# default/library definitions
include $(FINESS)/lib/3d/Makefile.defs

# Default (application level) definintions
include $(FINESS)/apps/3d/burgers/lib/Makefile.defs

# compiler options -- see file $(FINESS)/config/Makefile.config

## ------------------------------------------------------------------------- ##
# overridden library objects
#
AuxFunc         = AuxFunc
## ------------------------------------------------------------------------- ##

ALL_OBJECTS = \
$(COMMON_OBJECTS) \

ALL_SOURCES = \
$(COMMON_SOURCES) \

### targets ###

include $(FINESS)/lib/3d/Makefile.targets

### DO NOT remove this line - make depends on it ###
